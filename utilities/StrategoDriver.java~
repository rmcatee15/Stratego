import utilities.Board;
import utilities.Piece;
public class StrategoDriver{
  public static void main(String[] args){
    Board grid = new Board(8,10);
    grid.prepBoard();
    grid.print();
    Piece test = new Piece(5,1,1);
    grid.draw(test.getRow(),test.getColumn());
    grid.print();
  }
}